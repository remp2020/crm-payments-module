<button type="button" class="btn btn-xs btn-default">
    <i class="fa-solid fa-hand-holding-dollar"></i>
    <span
        title="{if $payment->additional_type == 'recurrent'}{_payments.admin.payments.donation_recurrent}{else}{_payments.admin.payments.donation}{/if}"
        data-toggle="tooltip"
    >
      {$payment->additional_amount|price}
    </span>
</button>